apiVersion: v1
kind: Service
metadata:
  name: media-tagger
  namespace: media-tagger
  labels:
    app.kubernetes.io/name: media-tagger
spec:
  selector:
    app.kubernetes.io/name: media-tagger
  ports:
    - name: http
      port: 80
      targetPort: http
